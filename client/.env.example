# Configuración del Cliente WebSocket Dashboard
# Este archivo es de referencia; la URL del servidor se configura via URL query parameter

# SERVIDOR WEBSOCKET DEL COMPAÑERO
# Especificar la IP/puerto del servidor WebSocket que tu compañero está ejecutando
# 
# Ejemplos:
#   localhost:9000          (si está en la misma máquina)
#   192.168.1.100:9000      (IP en la red local)
#   servidor.local:9000     (hostname DNS)
#
# USO: Abre http://localhost:8080/?server=IP:PUERTO
#
# Ejemplos de URLs completas:
#   http://localhost:8080/?server=localhost:9000
#   http://localhost:8080/?server=192.168.1.100:9000
#   http://localhost:8080/?server=mi-servidor.com:9000

WEBSOCKET_SERVER=localhost:9000

# PROTOCOLO
# ws   = inseguro (HTTP)
# wss  = seguro (HTTPS) - requiere certificados SSL
WEBSOCKET_PROTOCOL=ws

# AUTO-RECONNECT
# Reintentar conexión automáticamente si se desconecta
AUTO_RECONNECT=true
RECONNECT_INTERVAL=5000  # milisegundos

# MÁXIMO DE ALERTAS EN MEMORIA
# Las alertas antiguas se descartan para no sobrecargar el navegador
MAX_ALERTS=500

# DEBUG
# Mostrar logs detallados en consola
DEBUG=false
